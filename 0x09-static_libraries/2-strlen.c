#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - return length of an input string
 * @s: the input string
 * Return: the length of that string
 */

int _strlen(char *s)
{
	return  (strlen(s));
}

#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two numbers
 * @a: the first integer
 * @b: the second integer
 * Return: the result of thir multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

#include "main.h"
#include <ctype>
/**
 * *string_toupper - change chars to uppercase
 * @str: the string
 * Return: upper case string
 */
char *string_toupper(char *str)
{
	str = toupper(str);

	return (str);
}

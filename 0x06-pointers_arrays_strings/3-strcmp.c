#include "main.h"
#include <string.h>
/**
 * _strcmp - compair between 2 strings
 * @s1: the first string
 * @s2: the second string
 * Return: the number of different characters if there are diff chars
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
